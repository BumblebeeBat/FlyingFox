-module(channel_block_tx).
            %put all this into channel_block_tx:doit
            %N1 = Tx#channel_block.addressInt1,
            %N2 = Tx#channel_block.addressInt2,
            %Acc1 = block_tree:account(N1, ParentKey, Accounts),
            %Acc2 = block_tree:account(N2, ParentKey, Accounts),
            %true = sign:verify_both(Tx, Acc1#acc.pub, Acc2#acc.pub),
