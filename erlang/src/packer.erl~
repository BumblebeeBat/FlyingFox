-module(packer).
-export([pack/1,unpack/1]).

unpack(JSON) ->
    R = list_to_tuple(jiffy:decode(JSON)),
    F = element(1, R),
    G = list_to_atom(binary_to_list(F)),
    setelement(1, R, G).

pack(Record) ->
    J = jiffy:encode(tuple_to_list(Record)).
